# ─────────────────────────────────────────────────────────────────
# requirements-dev.txt — Local development dependencies
#
# NOTE: apache-airflow is intentionally excluded here.
#       It runs inside Docker (see docker/docker-compose.yml).
#       You write DAG files locally but Airflow executes them in-container.
#
# NOTE: PySpark requires Java 11+ to be installed on your machine.
#       Run: java -version  to check. If missing, install from:
#       https://adoptium.net/
# ─────────────────────────────────────────────────────────────────

# Data processing (core engine for all transformations)
pyspark==3.5.1

# Data ingestion
yfinance==0.2.38
requests==2.32.3

# Kafka (streaming producer + consumer)
confluent-kafka==2.4.0
fastavro==1.9.4          # Avro serialization/deserialization

# Storage (MinIO / S3 client)
boto3==1.34.144
minio==7.2.7

# Data quality
great-expectations==0.18.16

# Utilities
python-dotenv==1.0.1     # Load .env files for local config
pydantic==2.7.4          # Data validation / schema models
tenacity==8.3.0          # Retry logic for API calls

# Testing
pytest==8.2.2
pytest-cov==5.0.0
chispa==0.9.4            # PySpark DataFrame comparison in tests

# Linting / formatting
ruff==0.4.9

# Notebook (for Gold layer exploration)
jupyter==1.0.0
pandas==2.2.2
matplotlib==3.9.0
pyarrow==16.1.0          # Required for Iceberg + Parquet reads
